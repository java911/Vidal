{% extends 'VidalMainBundle::layout.html.twig' %}


{% block title %}
    <h1>Шаг второй - выберите врача</h1>
{% endblock %}

{% block content %}
    {% if specialties is not empty %}
            <div class="spec-box">
            {% for doc in doctors %}
                <div id="{{ doc.id }}">{{ doc.name }}</div>
            {% endfor %}
            </div>
    {% else %}
        Врачи отсутствуют
    {% endif %}
{% endblock %}




{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .spec-box{
            width: 280px;
        }
        .spec-box div{
            margin-top: -1px;
            border: 1px solid #cc0000;
            padding: 5px 10px;
        }

        .spec-box div:hover{
            background: #eeeeee;
            cursor: pointer;
        }
    </style>
{% endblock %}