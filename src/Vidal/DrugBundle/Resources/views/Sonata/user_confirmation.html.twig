<td class="swap">
	<a class="btn btn-swap {{ object.confirmation ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_main', {'field':'confirmation', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>