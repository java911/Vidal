{% extends ('VidalMainBundle::layout.html.twig') %}


{% block content %}
	<div style="text-align: center; margin-left: 255px;">
		{% if status_code == 404 %}
			<div class="not-found">
				<img src="{{ asset('bundles/vidalmain/images/not_found.jpeg') }}"/>

				<div class="not-found-content">
					<ul>
						<li>Возможно, неправильно набран адрес страницы</li>
						<li>Или старая ссылка более не существует<br>
							(страница была удалена или поменяла адрес)
						</li>
					</ul>

					<p>Если вы уверены, что страница должна быть по этому адресу, пожалуйста,
						сообщие нам об этом через форму <a href="{{ path('contacts') }}">обратной связи</a>.</p>
				</div>
			</div>
		{% elseif status_code == 403 %}
			<table style="width: 600px; margin-left: 255px;" align='center'>
				<tr>
					<td style="text-align: center; padding-left: 10px;">
						<h3> У вас нет доступа к этой части сайта.
							{% if app.user.type == 'doctor' %}

								<br/><br/>
								<div style="text-align: left;">
									<ul style="font-weight: normal">
										<li>Сюда имеют доступ только врачи, а вы не прошли тест или не подтвердили свой
											e-mail адрес, зайдите в свой
											<a href="{{ path('edit_profile') }}" style='text-decoration: underline;'>
												профайл </a> чтобы узнать точную причину
										</li>
										<br/>
										<li>Если ваш статус врача подтвержден, но вы все равно видите это сообщение,
											значит у вас и правда нет доступа сюда. Если вы считаете что доступ у вас
											должен быть,
											обратитесь в редакцию сайта с помощью <a href="{{ path('contacts') }}"
																					 style='text-decoration: underline;'>формы
												обратной связи</a>
										</li>
									</ul>
								</div>
								<br/>
							{% else %}
								<br/>
								<br/>У вас нет доступа сюда. Если вы считаете что доступ у вас должен быть, обратитесь в редакцию сайта с помощью
								<a href="{{ path('contacts') }}" style='text-decoration: underline;'>формы обратной
									связи</a>
							{% endif %}
						</h3>
					</td>
				</tr>
			</table>
		{% else %}
			<h1 style="text-align: center;">К сожалению, возникли трудности:</h1><br/><br/>
			<h3>Ваш запрос сейчас не может быть обработан. Мы в курсе этой ошибки и работаем над её устранением.
				Попробуйте заглянуть чуть позже</h3>
		{% endif %}
	</div>
{% endblock %}


{% block stylesheets %}
	{{ parent() }}
	<style type="text/css">
		ul li {
			list-style-type: none;
		}
		.not-found {
			padding-left: 275px;
			width:        700px;
			margin:       45px auto 10px;
			overflow:     hidden;
		}
		.not-found img {
			float: left;
			width: 200px;
		}
		.not-found-content {
			margin-left: 234px;
			margin-top:  36px;
		}
		.not-found-content p {
			font-weight: bold;
		}
		li {
			list-style-type: circle;
		}
	</style>
{% endblock %}
